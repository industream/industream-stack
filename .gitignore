.env
.env.prod
.env.dev
.env.staging
.env.secure

# Docker Secrets (local backup)
secrets/
*.secret
config/gotify/token

# Generated files
SIMULATORS-*.md
docker-stack-resolved*.yml
docker-stack-secrets.yml
docker-stack.workers-legacy.yml
traefik-dynamic/traefik-dynamic.yml

# Generated per-env at deploy time by create-secrets.sh — never commit the real
# file (it would leak a default htpasswd hash). Keep only the .example.
traefik-dynamic/backup-monitor-htpasswd
certs/
client-setup/
config/worker-versions.json
config/uifusion/config.json
config/cloudbeaver/data-sources.json
industream-cli/

# Per-installation deploy-state repo (its OWN nested git repo, managed by
# unified/scripts/deploy-state.sh — never tracked by the platform clone)
.deploy-state/
